services:
  - type: web
    name: world-cup-path
    env: node
    region: oregon
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
      - key: FOOTBALL_DATA_API_KEY
        sync: false
